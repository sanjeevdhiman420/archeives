/**
 * Basic operators, maths:-
 * Terms: “unary”, “binary”, “operand”
 * for instance a[opeand] +[opreator] b[opeand]  and called binary  and have double operand 
 * for instance (-a)  and called unary and have single operand 
 */

//Unary expression
let a = -x;
let b = +y;
//Binary expression
let  c = a + b;
let  d = b + c;

//String concatenation with binary +
console.log("hello"+" "+ "world"); //result hello world
console.log("hello"+ 1 + "world"); //result hello1world
